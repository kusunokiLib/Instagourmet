<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>名前</th>
      <th>email</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @user.id %></td>
      <td><%= @user.name %></td>
      <td><%= @user.email %></td>    
    </tr>
  </tbody>
</table>

<% @posts.each do |post| %>
  <p><%= post.text %></p>
<% end %>